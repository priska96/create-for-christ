NODE_ENV=development
HOST=0.0.0.0
PORT=3001
DATABASE_URL=postgresql://cfc:cfc_local_only@127.0.0.1:5433/create_for_christ
CORS_ORIGINS=http://localhost:8081,http://127.0.0.1:8081

# Use the same reachable host as EXPO_PUBLIC_API_URL (LAN IP for phones).
AUTH_BASE_URL=http://localhost:3001
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
BETTER_AUTH_SECRET=replace-with-a-random-secret-of-at-least-32-characters
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_SECURE=false
MAIL_FROM=Create For Christ <noreply@createforchrist.local>
# SMTP_USER=
# SMTP_PASSWORD=
